[functions]
  directory = "server"
  node_bundler = "esbuild"

[build]
  command = "npm install && cd server && npm install && cd ../front && npm install && npx expo export"
  publish = "front/dist"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200
