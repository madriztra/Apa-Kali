[functions]
  directory = "server"
  node_bundler = "esbuild"

[build]
  command = "cd front && npx expo export"
  publish = "front/dist"
  
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200
